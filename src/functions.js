// use if & else statements, return 'even' / 'odd' based on the input number
function checkNumber(num) {
    return 'Not implemented';
}

// use ternary operator, return 'even' / 'odd' based on the input number
function checkNumber2(num) {
    return 'Not implemented';
}

// use ternary operator, return the larger number or the same number if equal
function findMax(a, b) {
    return 'Not implemented';
}

// use if-else statements, return 'active' for 1, 'pending' for 2, 'unknown' for all other values
function getStatus(code) {
    return 'Not implemented';
}

// use switch-case statements, return 'active' for 1, 'pending' for 2, 'unknown' for all other values
function getStatus2(code) {
    return 'Not implemented';
}

// use switch(true), return 'low' for num < 10, 'medium' for 10 <= num < 20, 'high' for num < 30, 'unknown' for all other values
function switchExample(num) {
return 'Not implemented';
}

// use for loop to check if a number is prime
function isPrime(n) {
return 'Not implemented';
}

// use while loop to check if a number is prime
function isPrime2(n) {
    return 'Not implemented';
}

// use for loop to sum numbers up to n, excluding number 7
function loopExample(n) {
return 'Not implemented';
}

// this function should return true iff a is equal to b and both are of the same type, otherwise return false
function strictEquality(a, b) {
return 'Not implemented';
}

// this function should return true iff a is equal to b (not has to be on the same type), otherwise return false
function looseEquality(a, b) {
return 'Not implemented';
}

// This function should return the value if it is not null or undefined, otherwise return 'default'
function specialPhrases1(value) {
return 'Not implemented';
}

// This function should convert a value to its boolean equivalent
function specialPhrases2(value) {
return 'Not implemented';
}

// This function should return a if it's truthy, otherwise return b
function specialPhrases3(a, b) {
return 'Not implemented';
}

module.exports = { checkNumber,checkNumber2, findMax, getStatus,getStatus2, switchExample, isPrime,isPrime2, loopExample, strictEquality,looseEquality, specialPhrases1,specialPhrases2, specialPhrases3 };
